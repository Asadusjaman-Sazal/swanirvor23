<?xml version="1.0" encoding="utf-8"?>
<!--
    This app stores a sensitive savings ledger, so no user data should ever
    leave the device through a backup. Exclude every domain so that legacy full
    backups (adb backup) and Auto Backup on Android 11 and lower carry nothing.
-->
<full-backup-content>
    <exclude domain="root" path="."/>
    <exclude domain="file" path="."/>
    <exclude domain="database" path="."/>
    <exclude domain="sharedpref" path="."/>
    <exclude domain="external" path="."/>
    <exclude domain="device_root" path="."/>
    <exclude domain="device_file" path="."/>
    <exclude domain="device_database" path="."/>
    <exclude domain="device_sharedpref" path="."/>
</full-backup-content>
